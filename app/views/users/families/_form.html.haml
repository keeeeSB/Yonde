= simple_form_for(family, url: family_path) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name,
              required: true,
              autofocus: true

  #children-fields
    = f.simple_fields_for :children do |child_form|
      = render 'users/families/child_fields', f: child_form

  %button.btn.btn-sm.btn-success.mt-2#add-child
    + お子様を追加

  .form-actions.text-center
    = f.button :submit, class: 'btn btn-primary mt-3'
